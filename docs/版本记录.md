# 版本记录

每次打包发布在此登记一行：版本号 + 日期 + 本次包含的主要内容。
版本号改动位置：`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`package.json`
（三处保持一致；设置页底部的「V 主.次」自动跟随，无需手改）。

| 版本  | 日期       | 主要内容                                                                                       |
| ----- | ---------- | ---------------------------------------------------------------------------------------------- |
| 1.5.0 | 2026-09-14 | 窗口模式新增「最小化到托盘」底栏按钮（设置右侧）：藏起主窗口、任务栏不显示，托盘左键/菜单、全局热键、提醒通知点击均可唤回；首次触发弹系统提示通知（settings 落库只弹一次）；toast 通知线程消息扩为任务提醒/托盘提示两种 |
| 1.4.0 | 2026-09-09 | 设置项调整：收回延时下限 0.5s→0.1s（滑出本就 0.1s）；新增「面板透明度」滑杆 60%~95% 无极调节，贴边透明、窗口普通透明与收起细条共用一个值（默认 70%，实体与窗口亚克力不适用；窗口普通透明默认由 50% 抬至 70%） |
| 1.3.0 | 2026-09-08 | 合并 feature/task-repeat（M4）：任务重复（不重复/每天/每周/每月，勾选结算旧卡+新建顺延新卡，完成记录表驱动周报，重复行常驻弹层、↻ 标记，存量完成记录迁移回填）；应用图标全套替换（Win11 渐变对勾新图）；系统托盘（左键切换面板、右键展开/收起+打开设置+退出） |
| 1.2.0 | 2026-09-04 | 合并 feature/window-mode：新增「运行模式」贴边/窗口切换；窗口模式系统亚克力背景（Win11 实时模糊+系统圆角）、普通透明、自由缩放、隐私锁（失焦自动上锁/手动锁/严格解锁）、图钉置顶；删除毛玻璃旧外观 |
| 1.2.0-beta.4 | 2026-09-04 | 新增窗口模式隐私锁：失焦满时长自动上锁（Rust 计时，默认1分钟可调可关）、底栏锁头立即上锁、不透明锁屏（大锁+点击显示内容，严格解锁）、锁屏兼拖动区；图钉改共享状态：窗口模式=置顶开关、贴边=防自动收起，跨重启记忆 |
| 1.2.0-beta.3 | 2026-09-04 | 四角修复：亚克力材质启用系统圆角裁切（8px，面板 CSS 同步对齐），消除直角与外溢底色；删除「毛玻璃」外观（Win11 上黑底/卡顿），窗口背景仅剩亚克力与普通透明 |
| 1.2.0-beta.2 | 2026-09-04 | 实体机反馈修复：旧版 blur 接口在 Win11 黑底/拖动失效/掉帧，新增默认「系统亚克力」材质（Win11 DWM SystemBackdrop，微信同款实时模糊），存量毛玻璃设置自动迁移；云电脑等不支持环境继续用普通透明 |
| 1.2.0-beta | 2026-09-04 | 预发布（feature/window-mode 分支）：新增「运行模式」贴边/窗口切换；窗口模式毛玻璃或普通透明背景、自由缩放、任务栏图标；深浅/外观全兼容；贴边模式行为不变 |
| 1.1.0 | 2026-09-03 | 「今日已完成」分组；完成/删除撤销 toast（多条堆叠、15 秒）；全局动画总开关与行级/弹层全套动画 |
| 1.0.0 | 2026-09-03 | 首个正式版：贴边常驻、任务增删改与拖拽排序、提醒通知、周报、全局热键、外观/深浅模式、自启     |
