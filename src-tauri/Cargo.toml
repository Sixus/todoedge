[package]
name = "todoedge"
version = "1.4.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "todoedge_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-autostart = "2"
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.40.2", features = ["bundled"] }
chrono = "0.4.45"
tokio = { version = "1", features = ["macros", "time"] }
windows = { version = "0.61.3", features = [
  "Data_Xml_Dom",
  "Foundation",
  "UI_Notifications",
  "Win32_Foundation",
  "Win32_Graphics_Dwm",
  "Win32_System_Com",
  "Win32_System_LibraryLoader",
  "Win32_System_Registry",
  "Win32_UI_Input_KeyboardAndMouse",
  "Win32_UI_Shell",
  "Win32_UI_WindowsAndMessaging",
] }
tauri-plugin-clipboard-manager = "2.3.3"
# 窗口模式（设置里「运行模式=窗口」）的系统级背景材质：默认走 Win11 DWM
# SystemBackdrop 亚克力 + 系统圆角（window_ctl.rs 直接调 DwmSetWindowAttribute）；
# 贴边模式仍走原生逐像素透明（docs/01 第 7 节约定不变）
# 注：曾用的 window-vibrancy（accent blur/acrylic）在 Win11 上发黑、拖动失效
# 且掉帧，已随「毛玻璃」外观一并移除（实体机反馈 2026-09-04）

